<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630" width="1200" height="630" role="img" aria-label="umuo, curated links from across the web">
  <!-- Source for public/og.png, the default Open Graph card. Browsers do not
       accept SVG for og:image, so the PNG beside this file is what ships:
       rasterize this file with sips, writing the result to public/og.png.
       Palette mirrors the dark theme tokens: ground #0a0e0c, text #f2f7f4,
       muted #8a9690; the robot emoji carries its own colours. -->
  <rect width="1200" height="630" fill="#0a0e0c" />

  <!-- Brand mark: the robot emoji, matching the header logo and favicon. The
       emoji font supplies the artwork, so there is no vector to redraw. -->
  <text
    x="600"
    y="260"
    text-anchor="middle"
    font-size="200"
  >🤖</text>

  <text
    x="600"
    y="452"
    text-anchor="middle"
    font-family="Helvetica Neue, Helvetica, Arial, sans-serif"
    font-size="112"
    font-weight="bold"
    letter-spacing="1"
    fill="#f2f7f4"
  >umuo</text>

  <text
    x="600"
    y="520"
    text-anchor="middle"
    font-family="Helvetica Neue, Helvetica, Arial, sans-serif"
    font-size="36"
    fill="#8a9690"
  >Curated links from across the web</text>
</svg>
