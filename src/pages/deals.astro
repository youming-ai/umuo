---
/**
 * Deals Page
 * 特惠页面 - 限时折扣和促销商品
 */
import { Clock, Filter, Gift, Percent, Zap } from 'lucide-react';
import SearchBar from '../components/islands/SearchBar.jsx';
import { Button } from '../components/ui/button';
import ProductCard from '../components/ui/ProductCard.jsx';
import BaseLayout from '../layouts/BaseLayout.astro';
import { mockService } from '../lib/mock/service';

const featuredDeals = mockService.getFeaturedDeals();
const categories = mockService.getCategories();
---

<BaseLayout
  title="特別セール・お得な情報 - UMUO"
  description="限定割引、タイムセール、クーポン情報など、お得な買い物情報をご紹介。最大70%OFFの特別価格を見逃さないでください。"
>
  <!-- Deals Hero -->
  <section class="relative pt-24 pb-32 overflow-hidden bg-gray-900">
    <div class="absolute inset-0 opacity-20">
      <div
        class="absolute top-0 left-0 w-full h-full bg-[radial-gradient(circle_at_50%_120%,rgba(59,130,246,0.5),transparent_50%)]"
      >
      </div>
    </div>
    <div class="container relative z-10 text-center">
      <div
        class="inline-flex items-center px-4 py-2 rounded-full bg-red-500/10 text-red-500 text-sm font-black mb-8 animate-pulse-gentle border border-red-500/20"
      >
        <Zap className="w-4 h-4 mr-2 fill-red-500" />
        HOT DEALS TODAY
      </div>
      <h1
        class="text-5xl md:text-7xl font-black text-white mb-8 tracking-tighter"
      >
        今日一番の<br class="md:hidden" />「ヤバい」を。<br />
        <span
          class="text-transparent bg-clip-text bg-gradient-to-r from-red-500 via-orange-500 to-yellow-500"
        >
          最大 70% OFF
        </span>
      </h1>
      <p class="text-xl text-gray-400 mb-12 max-w-2xl mx-auto font-medium">
        AIが主要ECサイトから割引率の高い商品を24時間リアルタイムで抽出。<br />
        掘り出し物を逃さずチェック。
      </p>
      <div class="max-w-xl mx-auto">
        <SearchBar client:load placeholder="セール商品を検索..." />
      </div>
    </div>
  </section>

  <div class="container py-24">
    <div class="flex flex-col lg:flex-row gap-16">
      <!-- Sidebar -->
      <aside class="lg:w-80 flex-shrink-0">
        <div class="sticky top-24 space-y-8">
          <div
            class="bg-white rounded-[2.5rem] shadow-xl shadow-gray-100 p-8 border border-gray-100"
          >
            <h3
              class="text-xl font-black text-gray-900 mb-8 flex items-center gap-3"
            >
              <Filter className="w-6 h-6 text-primary-600" />
              絞り込み
            </h3>

            <div class="space-y-6">
              <div>
                <span
                  class="text-xs font-black text-gray-400 uppercase tracking-widest mb-4 block"
                  >Categories</span
                >
                <div class="flex flex-col gap-2">
                  <button
                    class="flex items-center justify-between p-4 rounded-2xl bg-gray-900 text-white font-bold transition-all shadow-lg shadow-gray-900/10"
                  >
                    <span>すべて</span>
                    <span class="text-xs opacity-50">{featuredDeals.length}</span>
                  </button>
                  {
                    categories.map((cat) => (
                      <button class="flex items-center justify-between p-4 rounded-2xl bg-white text-gray-600 font-bold border border-gray-100 hover:bg-gray-50 transition-all">
                        <span>{cat.name}</span>
                        <span class="text-xs text-gray-400">{cat.count}</span>
                      </button>
                    ))
                  }
                </div>
              </div>

              <div class="pt-6 border-t border-gray-100">
                <span
                  class="text-xs font-black text-gray-400 uppercase tracking-widest mb-4 block"
                  >Sort By</span
                >
                <select
                  class="w-full h-14 bg-gray-50 border-none rounded-2xl px-6 font-bold text-gray-900 focus:ring-2 focus:ring-primary-500 appearance-none"
                >
                  <option>割引率の高い順</option>
                  <option>価格の安い順</option>
                  <option>終了間近</option>
                  <option>おすすめ順</option>
                </select>
              </div>
            </div>
          </div>

          <!-- Alert Premium -->
          <div
            class="bg-gradient-to-br from-red-600 to-orange-600 rounded-[2.5rem] p-8 text-white shadow-2xl shadow-red-500/20 relative overflow-hidden"
          >
            <div
              class="absolute -top-12 -right-12 w-32 h-32 bg-white/10 rounded-full blur-2xl"
            >
            </div>
            <div class="relative z-10">
              <Gift className="w-12 h-12 mb-6" />
              <h3 class="text-2xl font-black mb-3">限定クーポン入手</h3>
              <p
                class="text-red-100 text-sm font-bold opacity-80 leading-relaxed mb-8"
              >
                会員登録で、Amazonや楽天で使えるシークレットクーポンを定期的にお届けします。
              </p>
              <Button
                asChild
                class="w-full py-4 bg-white text-red-600 rounded-[1.5rem] font-black hover:scale-[1.02] transition-transform shadow-xl"
              >
                <a href="/register">無料で会員登録</a>
              </Button>
            </div>
          </div>
        </div>
      </aside>

      <!-- Main Results -->
      <main class="flex-1">
        <div class="flex items-center justify-between mb-12">
          <h2 class="text-3xl font-black text-gray-900 flex items-center gap-4">
            <Percent className="w-8 h-8 text-red-500" />
            期間限定セール
          </h2>
          <div
            class="flex items-center gap-2 text-sm font-bold text-gray-400 uppercase tracking-widest"
          >
            <div class="flex items-center gap-2">
              Live updating
              <span class="flex h-2 w-2 relative">
                <span
                  class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"
                ></span>
                <span class="relative inline-flex rounded-full h-2 w-2 bg-red-500"
                ></span>
              </span>
            </div>
          </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-2 gap-8 mb-16">
          {
            featuredDeals.map((product) => (
              <ProductCard product={product} client:load />
            ))
          }
        </div>

        <!-- Newsletter Premium -->
        <section
          class="relative bg-gray-900 rounded-[3.5rem] p-12 md:p-20 overflow-hidden text-center"
        >
          <div
            class="absolute inset-0 bg-gradient-to-br from-primary-600/20 to-purple-600/20"
          >
          </div>
          <div class="relative z-10 max-w-2xl mx-auto">
            <h3 class="text-4xl font-black text-white mb-6 tracking-tight">
              ヤバいお得情報、<br />見逃してませんか？
            </h3>
            <p class="text-gray-400 text-lg mb-12 leading-relaxed">
              セールが終わる前に通知を受け取ろう。毎日10時にお得なサマリーを配信中。
            </p>
            <div class="flex flex-col sm:flex-row gap-4">
              <input
                type="email"
                placeholder="メールアドレス"
                class="flex-1 h-16 bg-white/10 border-2 border-white/10 rounded-2xl px-8 text-white font-bold outline-none focus:border-primary-500 transition-colors placeholder:text-gray-500"
              />
              <Button
                class="h-16 px-10 bg-white text-black font-black rounded-2xl hover:bg-primary-500 hover:text-white transition-all active:scale-95 shadow-xl shadow-white/5"
              >
                登録する
              </Button>
            </div>
          </div>
        </section>
      </main>
    </div>
  </div>
</BaseLayout>
