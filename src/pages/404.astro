---
import { ArrowLeft, ChevronRight, Home, Search, Zap } from 'lucide-react';
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="ページが見つかりません - UMUO"
  description="お探しのページは見つかりませんでした。"
>
  <div class="min-h-[80vh] flex items-center justify-center py-24">
    <div class="container relative">
      <!-- Background Decorative Elements -->
      <div
        class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-4xl h-96 bg-primary-500/10 rounded-full blur-[120px] -z-10 animate-pulse-gentle"
      >
      </div>

      <div class="max-w-4xl mx-auto text-center relative z-10">
        <div class="inline-flex items-center gap-4 mb-12">
          <div class="w-24 h-px bg-gradient-to-r from-transparent to-gray-200">
          </div>
          <span
            class="text-xs font-black text-primary-600 uppercase tracking-[0.5em] animate-fade-in"
            >Error Code 404</span
          >
          <div class="w-24 h-px bg-gradient-to-l from-transparent to-gray-200">
          </div>
        </div>

        <h1
          class="text-[12rem] md:text-[20rem] font-black leading-none text-gray-900 tracking-tighter mb-8 animate-slide-up"
        >
          404
        </h1>

        <h2
          class="text-4xl md:text-6xl font-black text-gray-900 mb-8 tracking-tight animate-slide-up delay-100 italic"
        >
          「ヤバい」、<br class="md:hidden" />見つからない。
        </h2>

        <p
          class="text-xl text-gray-500 font-medium max-w-lg mx-auto mb-16 animate-slide-up delay-200"
        >
          お探しのページは、お得すぎて売り切れたか、<br
            class="hidden md:block"
          />
          もともと存在しない幻のページのようです。
        </p>

        <div
          class="flex flex-col sm:flex-row gap-6 justify-center items-center animate-slide-up delay-300"
        >
          <a
            href="/"
            class="group w-full sm:w-auto h-20 px-12 bg-gray-900 text-white rounded-[1.5rem] font-black flex items-center justify-center gap-4 shadow-2xl shadow-gray-900/20 hover:scale-[1.05] transition-all"
          >
            <Home className="w-6 h-6" />
            トップへ戻る
          </a>
          <a
            href="/search"
            class="group w-full sm:w-auto h-20 px-12 bg-white text-gray-900 rounded-[1.5rem] font-black flex items-center justify-center gap-4 border border-gray-100 shadow-xl shadow-gray-200/50 hover:bg-gray-50 transition-all hover:scale-[1.05]"
          >
            <Search
              className="w-6 h-6 group-hover:text-primary-600 transition-colors"
            />
            商品を探す
          </a>
        </div>

        <!-- Quick Links -->
        <div
          class="mt-24 grid grid-cols-2 md:grid-cols-4 gap-6 animate-fade-in delay-500"
        >
          {
            ["価格比較", "セール情報", "カテゴリー", "会員登録"].map(
              (tag, idx) => (
                <a
                  href={`/${["compare", "deals", "categories", "profile"][idx]}`}
                  class="bg-white/50 backdrop-blur-md rounded-2xl p-6 border border-white hover:bg-white hover:border-primary-500 transition-all group"
                >
                  <div class="text-xs font-black text-gray-400 uppercase tracking-widest mb-2 group-hover:text-primary-600">
                    Quick Link
                  </div>
                  <div class="text-sm font-black text-gray-900 flex items-center justify-between">
                    {tag}
                    <ChevronRight className="w-4 h-4 text-gray-300 group-hover:translate-x-1 transition-transform" />
                  </div>
                </a>
              ),
            )
          }
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
