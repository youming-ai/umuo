---
import { ArrowRight } from 'lucide-react';
import { Button } from '../ui/button';
---

<!-- CTA Section -->
<section class="py-16 md:py-24">
  <div class="container">
    <div
      class="relative rounded-[2.5rem] md:rounded-[3rem] overflow-hidden bg-gray-900 p-8 md:p-16 lg:p-20 text-center"
    >
      <div
        class="absolute inset-0 bg-gradient-to-br from-gray-900 via-gray-800 to-primary-900 opacity-95"
      >
      </div>
      <div
        class="absolute -top-12 -left-12 w-48 h-48 md:w-96 md:h-96 bg-primary-500 rounded-full blur-[100px] opacity-20"
      >
      </div>
      <div
        class="absolute -bottom-12 -right-12 w-48 h-48 md:w-96 md:h-96 bg-secondary-500 rounded-full blur-[100px] opacity-20"
      >
      </div>

      <div class="relative z-10 max-w-3xl mx-auto">
        <h2
          class="text-3xl md:text-4xl lg:text-5xl font-extrabold tracking-tight text-white mb-6 md:mb-8 leading-tight"
        >
          賢い買い物で、<br />人生を豊かにしよう。
        </h2>
        <p class="text-lg md:text-xl text-gray-300 mb-8 md:mb-12 leading-relaxed">
          価格アラートやAI分析など、すべての機能が無料で使えます。<br />
          まずは商品を検索することから始めましょう。
        </p>
        <div
          class="flex flex-col sm:flex-row items-center justify-center gap-4 md:gap-6"
        >
          <Button
            asChild
            size="lg"
            className="w-full sm:w-auto rounded-2xl px-8 md:px-12 shadow-xl shadow-primary-500/20"
          >
            <a href="/register"> 無料でアカウント作成 </a>
          </Button>
          <Button
            asChild
            variant="outline"
            size="lg"
            className="text-white border-white/20 hover:bg-white/10 w-full sm:w-auto rounded-2xl px-8 md:px-12"
          >
            <a href="#products">
              商品を探す
              <ArrowRight className="w-5 h-5 ml-2" />
            </a>
          </Button>
        </div>
      </div>
    </div>
  </div>
</section>
